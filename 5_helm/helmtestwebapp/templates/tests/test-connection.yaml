apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmtestwebapp.fullname" . }}-test-connection"
  labels:
{{ include "helmtestwebapp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helmtestwebapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
