apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "TestWebApp.fullname" . }}-test-connection"
  labels:
{{ include "TestWebApp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "TestWebApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
